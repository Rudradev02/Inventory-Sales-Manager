Build a complete, polished **Inventory & Sales Management System** using Django.

I need this project finished quickly, so this is an MVP for a portfolio/resume. **Do not over-engineer it. Do not add unnecessary features or technologies. Prioritize a working application, clean UI, and reliable core functionality.**

## Tech Stack

* Python 3
* Django
* SQLite
* HTML5
* CSS3
* Bootstrap 5
* Chart.js
* Django ORM
* Django built-in authentication

Do NOT use React, Node.js, PostgreSQL, Redis, Celery, Docker, or external APIs.

## Project Name

`inventory_sales_manager`

## Main Features

### 1. Authentication

Implement:

* Login
* Logout
* Register
* Protect dashboard and management pages so only authenticated users can access them.

Use Django's built-in authentication system.

### 2. Dashboard

Create a modern dashboard showing:

* Total Products
* Total Stock Units
* Low Stock Products
* Today's Sales
* Today's Revenue
* Total Revenue

Add:

* Sales overview chart using Chart.js
* Recent sales table
* Low-stock products section

The dashboard should be the main page after login.

### 3. Product Management

Create a Product model with:

* name
* SKU
* category
* price
* stock_quantity
* low_stock_threshold
* description
* created_at
* updated_at

Implement:

* Add product
* Edit product
* Delete product
* View product list
* View product details
* Search products
* Filter by category
* Show low-stock status

Use Django forms with proper validation.

Prevent duplicate SKU values.

### 4. Sales Management

Create a Sale model with:

* product
* quantity
* unit_price
* total_price
* sold_by
* created_at

Implement:

* Create sale
* View sales history
* Search/filter sales
* View sale details

When a sale is created:

1. Check whether enough stock exists.
2. Reduce product stock automatically.
3. Calculate total price automatically.
4. Save the user who created the sale.
5. Prevent selling more units than available.

Use Django transactions where appropriate.

### 5. Categories

Create a simple Category model:

* name
* description

Allow products to belong to categories.

### 6. Admin Panel

Configure Django admin for:

* Products
* Categories
* Sales

Make the admin interface useful with:

* list_display
* search_fields
* list_filter
* ordering

### 7. UI/UX

Create a professional portfolio-quality interface.

Use Bootstrap 5.

Layout:

* Sidebar navigation
* Top navigation bar
* Dashboard cards
* Clean tables
* Responsive forms
* Buttons with clear actions
* Status badges
* Confirmation before deleting records
* Empty states when there is no data

Use a clean modern business dashboard style.

Navigation:

Dashboard
Products
Categories
Sales
Admin
Logout

Do not make the UI unnecessarily complicated.

### 8. Database

Use SQLite.

Create proper Django migrations.

Make sure the application automatically creates the database structure when migrations are run.

### 9. Sample Data

Create a Django management command:

`python manage.py seed_data`

It should create:

* Sample categories
* At least 8 sample products
* Different stock levels
* Sample sales
* A demo user

Do not hard-code fake data into templates.

### 10. Project Structure

Use a clean Django structure such as:

inventory_sales_manager/
manage.py
config/
accounts/
inventory/
sales/
templates/
static/
db.sqlite3
requirements.txt
README.md

Keep responsibilities separated between Django apps.

### 11. Security

Follow basic Django security practices:

* CSRF protection
* Authentication checks
* POST requests for modifications
* Django form validation
* No passwords stored manually
* No secrets hard-coded into source code

### 12. README

Create a professional README containing:

* Project overview
* Features
* Tech stack
* Screenshots section placeholder
* Installation instructions
* How to run migrations
* How to create a superuser
* How to seed sample data
* How to run the development server
* Example demo credentials
* Future improvements

Include commands such as:

`python -m venv venv`

`pip install -r requirements.txt`

`python manage.py migrate`

`python manage.py seed_data`

`python manage.py runserver`

### 13. Requirements

Create `requirements.txt` with the required dependencies.

Keep dependencies minimal.

### 14. Important Implementation Rules

* The application must actually run.
* Do not leave TODOs for core functionality.
* Do not create placeholder pages for required features.
* Do not generate unnecessary documentation before the application works.
* Test every major workflow.
* Fix errors before finishing.
* Make sure all URLs work.
* Make sure forms submit correctly.
* Make sure database migrations work.
* Make sure login/logout works.
* Make sure stock decreases after a sale.
* Make sure insufficient stock is rejected.
* Make sure dashboard statistics are calculated from the database.
* Make sure the application works after restarting Replit.

## Final Verification

After implementation:

1. Install dependencies.
2. Run migrations.
3. Create/seed demo data.
4. Start the Django server.
5. Test login.
6. Test product creation.
7. Test product editing/deletion.
8. Test creating a sale.
9. Verify stock decreases.
10. Test insufficient-stock validation.
11. Verify dashboard statistics.
12. Check responsive UI.
13. Fix any errors you encounter.

At the end, give me a concise summary containing:

* What was built
* Files/apps created
* How to run it
* Demo login credentials
* Any remaining limitations

Focus on **finishing a working project rather than adding extra features**.
